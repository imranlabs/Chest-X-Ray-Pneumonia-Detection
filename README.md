# Chest-X-Ray-Pneumonia-Detection
This is a project on Kaggle Chest X-Ray images(pneumonia). This project implements PCA, SMV to classify images(ROC_AUC:~0.92). A CNN network from scratch and transfer learning (Resnet-18) was implement and tested on the corresponding dataset(ROC_AUC : ~0.95).
